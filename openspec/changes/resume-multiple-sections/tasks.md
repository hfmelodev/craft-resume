## 1. Dados

- [x] 1.1 Declarar os arrays das seções múltiplas em `ResumeContentData`
- [x] 1.2 Valores padrão `[]` no formulário do currículo

## 2. Componentes

- [x] 2.1 Instalar `@hello-pangea/dnd`
- [x] 2.2 Criar `MultipleDragList` com `useFieldArray`, estado vazio e reordenação por arrastar
- [x] 2.3 Criar `MultiplesSections` com as sete seções e exibir no painel de informações

## 3. Próximos passos

- [ ] 3.1 Diálogo `ManageMultipleItemDialog` para adicionar e editar itens (ligar `onAdd`/`onEdit`)
- [ ] 3.2 Usar `field.id` como `key`/`draggableId` do `Draggable`
- [ ] 3.3 Renderizar as seções no preview

## 4. Verificação

- [x] 4.1 `tsc --noEmit`, `biome check` e `next build` sem erros
