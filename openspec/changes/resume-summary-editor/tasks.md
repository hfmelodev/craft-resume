## 1. Componentes

- [x] 1.1 Adicionar `ui/tooltip` (shadcn) e wrapper `Tooltip`
- [x] 1.2 Criar `ui/editor` com Tiptap (`StarterKit` + `TextAlign`) e `MenuBar`
- [x] 1.3 Criar `EditorField` ligado ao formulário

## 2. Seção de resumo

- [x] 2.1 Declarar `content.summary` em `ResumeContentData` com padrão `'<p></p>'`
- [x] 2.2 Criar `SummarySection` ("Sobre Você") e exibir no painel de informações

## 3. Próximos passos

- [~] 3.1 Seções múltiplas (redes sociais, experiências, formações, idiomas, habilidades, certificações, projetos)
- [ ] 3.2 Renderizar o resumo no preview, com sanitização do HTML

## 4. Verificação

- [x] 4.1 `tsc --noEmit`, `biome check` e `next build` sem erros
