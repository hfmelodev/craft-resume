## 1. Classes canônicas

- [x] 1.1 `ui/switch`: tamanhos, variante `group-has-focus-visible` e `translate-x-0` no RTL
- [x] 1.2 `ui/tooltip`: `calc` da seta e `rounded-xs`

## 2. Verificação

- [x] 2.1 `tsc --noEmit`, `biome check` e `next build` sem erros
- [x] 2.2 CSS gerado contém `calc(-50% - 2px)`
